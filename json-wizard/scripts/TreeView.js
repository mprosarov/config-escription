const TreeView = (function(){
    constructor(initialData)
    addNode(parentPath, key, value)
    deleteNode(path)
    updateNode(path, newKey, newValue)
    getData()
    render(rootElement) // Инициирует первичный рендеринг

})