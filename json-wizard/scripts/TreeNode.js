const TreeNode = (function(){
    constructor(data, key, path, treeViewInstance)
    render()  //HTMLElement  Возвращает готовый DOM-элемент
    handleExpandClick()
    handleEditClick()
    handleDeleteClick()
    updateElement(newData) 

})